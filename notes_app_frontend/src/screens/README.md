This folder contains React screen components.

HomeScreen17451.jsx renders the Home Screen built from the generated assets at /assets/home-screen-174-51.* without relocating those files. Styles and JS are referenced through small wrapper files in src/ so CRA includes them in the bundle.
